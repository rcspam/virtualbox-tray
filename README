                  #### virtualbox-tray   #####
                  
A simple tray bash script to launch your virtualbox guest hosts from tray for Linux
It display an icon in tray.

A left click to launch guest hosts.
A right clic to launch VirtualBox it-self

No need setting. It find guest hosts it-self.

### Dependences
  * YAD: sudo apt-get/yum install yad

### Installation  
  * Install OS icons in your "$HOME/.icons" directory: 
  * Install the script 'virtualbox-tray' in your PATH (ie: $HOME/bin)
    and if it is not the case, make it 'executable'
