#### virtualbox-tray   #####
A simple tray app to launch your virtualbox vm from tray
No need setting. It find vm it-self.
Just additional package 'yad'

# Installation
  * You must install 'yad':  sudo apt-get/yum install yad
  * Install icons in your "$HOME/.icons" directory:
  * Install the script 'virtualbox-tray' in your PATH (ie: $HOME/bin)
    and if it is not the case, make it 'executable'
